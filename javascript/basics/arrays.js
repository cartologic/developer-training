// Dealing with arrays:
var numbers = [1, 2, 3, 4, 5, 6, 7, 8,]
// get array length:
console.log(numbers.length)
// access values
console.log(numbers[0], numbers[3])
// add item to the beginning:
numbers.unshift(0)
// add item to the end
numbers.push(9)

// arrays may contain different data types 
const myArray = [person, manager, 10, "random string!", false, true, undefined, null]
